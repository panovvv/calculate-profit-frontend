# Copy to .env (gitignored). API_PROXY_TARGET is required for `npm start` (no default) — it's the
# backend `npm start` proxies /api to. Pick one:
# API_PROXY_TARGET=http://localhost:8080
# API_PROXY_TARGET=https://calculate-profit-backend.onrender.com
